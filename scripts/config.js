// config.js
export const TOTAL_PRODUCTS = 20_000_000;
export const BATCH_SIZE = 10_000;

// match your DB seed
export const CATEGORY_IDS = Array.from({ length: 100 }, (_, i) => i + 1);
export const BRAND_IDS = Array.from({ length: 50 }, (_, i) => i + 1);
