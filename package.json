{
	"name": "large-dataset-operations",
	"version": "1.0.0",
	"description": "",
	"main": "src/server.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "node --env-file=.env src/server.js",
		"dev": "node --watch --env-file=.env src/server.js",
		"gen": "node scripts/runAll.js",
		"db:setup": "mysql -u root -p < src/db/schema.sql",
		"db:seed": "mysql -u root -p products_db < src/db/seed.sql"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"type": "module",
	"dependencies": {
		"@faker-js/faker": "^10.4.0",
		"cors": "^2.8.6",
		"express": "^5.2.1",
		"express-rate-limit": "^8.3.2",
		"ioredis": "^5.10.1",
		"mysql2": "^3.22.0"
	},
	"devDependencies": {
		"@types/ioredis": "^4.28.10",
		"@types/node": "^25.6.0"
	}
}
